/**
 * Готовность светлой темы.
 *
 * Была `false`, пока в `app/globals.css` жили ~332 жёстких цвета вне блоков
 * токенов — в основном `rgba(255,255,255,0.0X)`, осмысленные только на тёмном
 * фоне. На светлом такие поверхности теряли разделение: сайдбар и карточки
 * оставались светлыми, а контентная область — тёмной.
 *
 * Переведено: белые вуали стали вуалью цветом текста темы
 * (`rgb(var(--ink-rgb) / α)`), цветные акценты — токенами палитры. Осталось 30
 * жёстких значений, и все обоснованы: белый текст на акцентных поверхностях и
 * затемняющие подложки модалок, которые должны быть тёмными в любой теме.
 */
export const LIGHT_THEME_READY = true;
