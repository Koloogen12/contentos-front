/**
 * Конфиг лендинга THE DRAFT.
 *
 * Хендофф требует: цены и лимиты — не хардкодом в разметке, опциональные блоки —
 * за флагом (кандидаты на A/B), CTA — единая точка входа в продукт.
 */

/** Куда ведут все CTA. `/try` создаёт preview-сессию без регистрации. */
export const CTA_PATH = "/try";

/**
 * Опциональные блоки: в прототипе скрывались тумблером «Короткая версия».
 * Здесь — флаги. Порядок остальных блоков при выключении не меняется.
 */
export const LANDING_BLOCKS = {
  recognize: true, // 6 — Узнаёшь себя?
  whyNow: true, // 7 — Почему сейчас
  change: true, // 11 — Что изменится
  author: true, // 13 — Кто за этим стоит
} as const;

export interface LandingPlan {
  id: string;
  name: string;
  price: string;
  per: string;
  desc: string;
  items: string[];
  cta: string;
  featured?: boolean;
}

/**
 * Тарифы. ⚠️ Цены не финальные — в спеке фигурировали 990/2490/4990 и
 * 1490/4900. Здесь значения из последнего макета; менять в одном месте.
 */
export const LANDING_PLANS: LandingPlan[] = [
  {
    id: "preview",
    name: "Preview",
    price: "0 ₽",
    per: "",
    desc: "Без регистрации. Один канвас, импорт голоса, все площадки. Чтобы увидеть свой первый пост.",
    items: ["1 канвас", "Импорт голоса из канала", "Все площадки"],
    cta: "Начать",
  },
  {
    id: "author",
    name: "Автор",
    price: "990 ₽",
    per: "/мес",
    desc: "Для того, кто публикуется сам и регулярно.",
    items: [
      "Неограниченные канвасы",
      "60 постов в месяц",
      "Контент-план и очередь",
      "Публикация в Telegram",
      "Карусели",
    ],
    cta: "Попробовать",
    featured: true,
  },
  {
    id: "studio",
    name: "Студия",
    price: "2 490 ₽",
    per: "/мес",
    desc: "Для агентств, ghostwriter'ов и команд.",
    items: [
      "5 профилей голоса",
      "300 постов в месяц",
      "Общие канвасы",
      "Приоритетная поддержка",
    ],
    cta: "Попробовать",
  },
];
