/**
 * Реальные ассеты лендинга.
 *
 * Пока пусто — на местах рендерятся подписанные плейсхолдеры (LandingSlot).
 * Чтобы заменить: положить файл в `public/landing/` и добавить строку сюда.
 * Формат: webp, `loading="lazy"` ставится автоматически.
 *
 * Что нужно снять (из README хендоффа, раздел «Что требует данных»):
 *   face-1..3   — три реальных фото пользователей для бейджа в hero, 96×96
 *   author      — фото Данила для блока «Кто за этим стоит», ~600×760
 *   slide-1..4  — настоящие отрендеренные слайды карусели (JPEG из продукта)
 */
export const LANDING_ASSETS: Record<string, string> = {
  // "face-1": "/landing/face-1.webp",
  // "face-2": "/landing/face-2.webp",
  // "face-3": "/landing/face-3.webp",
  // author: "/landing/author.webp",
  // "slide-1": "/landing/slide-1.webp",
};
